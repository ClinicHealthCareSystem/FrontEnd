import { StyleSheet } from "react-native";

export default StyleSheet.create({
    safe: {
    flex: 1,
    backgroundColor: '#fff',
  },
  body: {
    flex: 1,
    paddingHorizontal: 16,
    paddingTop: 8,
  },
})